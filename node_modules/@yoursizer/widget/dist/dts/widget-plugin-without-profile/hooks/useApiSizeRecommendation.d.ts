import { type MeasurementSet } from '../utils/hookUtils';
interface UseApiSizeRecommendationParams {
    gender: "male" | "female" | null;
    measurements: MeasurementSet;
    fitPreference: "slim" | "regular" | "relaxed";
    userEditedMeasurements: {
        chest: boolean;
        waist: boolean;
        hips: boolean;
    };
    brandId?: string;
    clothingType?: string;
}
interface UseApiSizeRecommendationReturn {
    getApiSizeRecommendation: () => Promise<{
        size: string;
        confidence: number;
        method?: string;
    } | null>;
}
export declare function useApiSizeRecommendation({ gender, measurements, fitPreference, userEditedMeasurements, brandId, clothingType }: UseApiSizeRecommendationParams): UseApiSizeRecommendationReturn;
export {};
