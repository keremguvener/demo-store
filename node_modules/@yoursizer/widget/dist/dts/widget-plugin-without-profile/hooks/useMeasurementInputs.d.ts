import React from 'react';
interface DebugData {
    heightWeight?: {
        fetchedData?: {
            measurements?: {
                chest: number;
                waist: number;
                hips: number;
            };
        };
    };
}
import { Measurements } from '../types';
interface UseMeasurementInputsParams {
    isMobile: boolean;
    measurements: Measurements;
    setMeasurements: React.Dispatch<React.SetStateAction<Measurements>>;
    useMetric: boolean;
    gender: 'male' | 'female' | null;
    getLocalJSONBasedLimits: (measurementType: 'chest' | 'waist' | 'hips') => {
        min: number;
        max: number;
        default: number;
    };
    setShowMeasurementGuide: (type: "chest" | "waist" | "hips" | null) => void;
    handleMorphChange: (morphName: string, value: number) => void;
    handleMorphChangeFromSlider: (morphName: string, sliderValue: number, jsonValue: number, sliderMin: number, sliderMax: number) => void;
    handleWeightMorphs: (weightValue: number) => void;
    debugData?: DebugData;
}
interface UseMeasurementInputsReturn {
    renderMeasurementInputs: (type: 'height' | 'weight' | 'chest' | 'waist' | 'hips') => React.ReactElement;
}
export declare function useMeasurementInputs({ isMobile, measurements, setMeasurements, useMetric, gender, getLocalJSONBasedLimits, setShowMeasurementGuide, handleMorphChange, handleMorphChangeFromSlider, handleWeightMorphs, debugData }: UseMeasurementInputsParams): UseMeasurementInputsReturn;
export {};
