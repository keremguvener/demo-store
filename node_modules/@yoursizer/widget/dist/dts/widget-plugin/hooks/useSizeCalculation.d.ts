import { Measurements, FitPreference } from '../types/index';
interface UseSizeCalculationProps {
    measurements: Measurements;
    fitPreference: FitPreference;
}
export declare function useSizeCalculation({ measurements, fitPreference }: UseSizeCalculationProps): {
    calculateSize: () => {
        size: string;
        confidence: number;
    };
};
export {};
