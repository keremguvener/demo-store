import { UserProfile } from '../lib/userProfileStorage';
export declare function convertMapToObject(map: Map<string, number>): {
    [key: string]: number;
};
export declare function convertObjectToMap(obj: {
    [key: string]: number;
}): Map<string, number>;
export declare function createProfileFromMeasurements(gender: "male" | "female", measurements: UserProfile['measurements'], fitPreference: UserProfile['fitPreference']): Partial<UserProfile>;
export declare function validateProfileData(profile: Partial<UserProfile>): boolean;
export declare function getProfileDisplayName(profile: UserProfile): string;
export declare function formatProfileMeasurements(measurements: UserProfile['measurements']): string;
/**
 * Calculate body type label based on measurement value and range
 * This function matches the slider label calculation logic
 */
export declare function getBodyTypeLabel(measurement: string | number, min: number, max: number, measurementType: 'chest' | 'waist' | 'hips'): string;
/**
 * Get measurement range for a specific measurement type
 * This should match the range used in the slider
 */
export declare function getMeasurementRange(measurementType: 'chest' | 'waist' | 'hips', debugData: any, getLocalJSONBasedLimits: (type: 'chest' | 'waist' | 'hips') => {
    min: number;
    max: number;
    default: number;
}): {
    min: number;
    max: number;
};
